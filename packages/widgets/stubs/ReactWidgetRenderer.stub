import React from 'react'
import { createRoot } from 'react-dom/client'

function {{ componentName }}({ config }) {
    return <><h2>{config.heading ?? 'Widget heading'}</h2><p>{config.description ?? 'Widget description'}</p></>
}

export default function mount{{ componentName }}({ host, props: initialProps }) {
    const root = createRoot(host)
    const update = (props) => root.render(<{{ componentName }} {...props} />)
    update(initialProps)

    return { update, destroy: () => root.unmount() }
}
